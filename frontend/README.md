# frontend/

Тонкий React-клиент. Чистый **схемо-рендерер**: сервер отдаёт уже вычисленную
дельту-схему, фронт рендерит типовыми виджетами (CLAUDE.md → «Фронт»).
Транспорт — JSON-RPC через API-шлюз. Новая услуга = ноль кода здесь.

Заготовка. Стек/сборка — TBD.
